# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

#### Интерфейс IProduct
Описывает структуру данных товара, которые отображаются в каталоге или в корзине покупателя.

Поля интерфейса:
`id: string` — уникальный идентификатор товара
`description: string` — текстовое описание товара
`image: string` — ссылка на изображение товара
`title: string` — название товара
`category: string` — категория товара
`price: number | null` — цена товара. Может быть null, если товар не имеет цены

#### Интерфейс IBuyer
Описывает структуру данных покупателя, которые пользователь указывает при оформлении заказа.

Поля интерфейса:
`payment: TPayment` — выбранный способ оплаты
`email: string` — электронная почта покупателя
`phone: string` — номер телефона покупателя
`address: string` — адрес доставки

### Модели данных

#### Класс Products
Отвечает за хранение и управление товарами. Также хранит товар, выбранный пользователем для подробного отображения.

Конструктор:
`constructor()` — создаёт экземпляр каталога товаров и инициализирует пустой массив товаров и выбранный товар.

Поля класса:
`products: IProduct[]` — массив всех товаров, полученных от сервера
`previewProduct: IProduct | null` — товар, выбранный пользователем для подробного просмотра

Методы:
`setProducts(products: IProduct[]): void` — сохраняет массив товаров, полученный с сервера
`getProducts(): IProduct[]` — возвращает массив всех товаров из модели
`getProductById(id: string): IProduct | undefined` — возвращает товар по переданному идентификатору
`setPreviewProduct(product: IProduct): void` — сохраняет товар для отображения подробной информации
`getPreviewProduct(): IProduct | null` — возвращает товар, выбранный для подробного просмотра

#### Класс Basket
Отвечает за хранение товаров, которые пользователь добавил в корзину для покупки.

Конструктор:
`constructor()` — создаёт экземпляр корзины и инициализирует пустой массив товаров.

Поля класса:
`items: IProduct[]` — массив товаров, добавленных пользователем в корзину

Методы:
`getItems(): IProduct[]` — возвращает массив товаров, находящихся в корзине
`addItem(product: IProduct): void` — добавляет товар в корзину
`removeItem(product: IProduct): void` — удаляет товар из корзины
`clear(): void` — очищает корзину и удаляет все товары
`getTotalPrice(): number` — возвращает общую стоимость всех товаров в корзине
`getCount(): number` — возвращает количество товаров в корзине
`hasProduct(id: string): boolean` — проверяет наличие товара в корзине по его идентификатору

#### Класс Buyer
Отвечает за хранение данных покупателя, которые вводятся пользователем при оформлении заказа.

Конструктор:
`constructor()` — создаёт экземпляр модели покупателя и инициализирует поля пустыми значениями.

Поля класса:
`payment: TPayment | null` — выбранный способ оплаты
`email: string` — электронная почта покупателя
`phone: string` — номер телефона покупателя
`address: string` — адрес доставки

Методы:
`setData(data: Partial<IBuyer>): void` — сохраняет данные покупателя. Позволяет обновлять одно или несколько полей без удаления уже сохранённых значений
`getData(): IBuyer` — возвращает объект со всеми данными покупателя
`clear(): void` — очищает все сохранённые данные покупателя
`validate(): Partial<Record<keyof IBuyer, string>>` — выполняет проверку корректности данных покупателя и возвращает объект с ошибками.

### Слой коммуникации

#### Класс ApiClient
Отвечает за взаимодействие приложения с сервером. Класс использует композицию и принимает объект, реализующий интерфейс IApi. Через него выполняются HTTP-запросы к серверу.

Конструктор:
`constructor(api: IApi)` — принимает объект, реализующий интерфейс IApi, который предоставляет методы get и post для выполнения HTTP-запросов.

Поля класса:
`api: IApi` — экземпляр класса, реализующего интерфейс IApi, через который выполняются запросы к серверу.

Методы:

`getProducts(): Promise<IProduct[]>` — выполняет GET запрос на эндпоинт /product/ и возвращает массив товаров.

`createOrder(order: IOrderRequest): Promise<IOrderResponse>` — выполняет POST запрос на эндпоинт /order/ и отправляет данные заказа на сервер.

### Представление (View)
Слой View отвечает за отображение интерфейса сайта, работу с DOM и генерацию событий пользовательских действий

#### Класс Header
Отвечает за блок шапки сайта (.header) и управление корзиной в шапке.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает корневой элемент шапки и брокер событий.

Поля класса:
`container: HTMLElement` — корневой DOM-элемент шапки.
`basketButton: HTMLButtonElement` — кнопка открытия корзины.
`counterElement: HTMLElement` — счётчик товаров.

Методы класса:
`counter: number (сеттер)` — обновляет количество товаров в корзине.

#### Класс Gallery
Отвечает за блок каталога и размещение карточек товаров.

Конструктор:
`constructor(container: HTMLElement)` — принимает корневой элемент каталога.

Поля класса:
`container: HTMLElement` — корневой DOM-элемент каталога.

Методы класса:
`catalogElements: HTMLElement[] (сеттер)` — заменяет содержимое каталога списком карточек.

#### Класс Modal
Контейнер модального окна. Не имеет наследников.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает корневой элемент модального окна и брокер событий.

Поля класса:
`container: HTMLElement` — корневой DOM-элемент модального окна.
`contentElement: HTMLElement` — контейнер для контента.
`closeButton: HTMLButtonElement` — кнопка закрытия.

Методы класса:
`content: HTMLElement (сеттер)` — заменяет контент модального окна.
`closeByEsc(event: KeyboardEvent): void` — обработчик закрытия по Escape, навешивается только при открытии.
`open(): void` — открывает модальное окно.
`close(): void` — закрывает модальное окно.

#### Класс Card
Отвечает за отображение карточки в модальном окне, задавая в ней данные названия, описания, изображение, катетогии товара и цены. 

Конструктор:
`constructor(container: HTMLElement)` — принимает корневой элемент карточки.

Поля класса:
`container: HTMLElement` — корневой DOM-элемент карточки.
`titleElement: HTMLElement` — заголовок товара.
`priceElement: HTMLElement` — цена товара.

Методы класса:
`title: string (сеттер)` — устанавливает название товара.
`price: number | null (сеттер)` — устанавливает цену товара.

#### Класс Form
Базовый класс для всех форм. Содержит общую логику отправки формы, генерации событий при вводе и отображения ошибок.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — принимает DOM-элемент формы и брокер событий.

Поля класса:
`container: HTMLFormElement` — корневой DOM-элемент формы.
`submitButtonElement: HTMLButtonElement` — кнопка отправки формы.
`errorElement: HTMLElement` — блок вывода ошибок.

Методы класса:
`valid: boolean (сеттер)` — управляет доступностью кнопки отправки.
`errors: string (сеттер)` — выводит текст ошибок в форме.

#### CardCatalog
Карточка для списка каталога. 

Конструктор:
`constructor(container: HTMLButtonElement, selected: ICardActions)` — принимает корневой элемент карточки и обработчик выбора.

Поля класса:
`categoryElement: HTMLElement` — элемент категории.
`imageElement: HTMLImageElement` — изображение товара.

Методы класса:
`category: string (сеттер)` — обновляет категорию и модификатор.
`image: string (сеттер)` — обновляет изображение.

#### CardPreview
Карточка предпросмотра товара.

Конструктор:
`constructor(container: HTMLElement, actions: ICardPreviewActions)` — принимает корневой элемент карточки и брокер событий.

Поля класса:
`categoryElement: HTMLElement` — элемент категории.
`imageElement: HTMLImageElement` — изображение товара.
`description: HTMLElement` — описание товара.
`cardButtonElement: HTMLButtonElement` — кнопка добавления в корзину.

Методы класса:
`category: string (сеттер)` — обновляет категорию и модификатор.
`image: string (сеттер)` — обновляет изображение.
`description: string (сеттер)` — обновляет описание товара.
`buttonTitle: string (сеттер)` — изменяет подпись кнопки.
`buttonDisabled: boolean (сеттер)` — переключает доступность кнопки.

#### CardBasket
Карточка товара в корзине.

Конструктор:
`constructor(container: HTMLElement, action: ICardBasketActions)` — принимает корневой элемент карточки и обработчик удаления.

Поля класса:
`index: HTMLElement` — порядковый номер товара в корзине.
`deleteButtonElement: HTMLButtonElement` — кнопка удаления товара.

Методы класса:
`index: number (сеттер)` — устанавливает номер товара в корзине.

#### Класс BasketView
Отвечает за содержимое корзины. 

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает корневой элемент корзины и брокер событий.

Поля класса:
`listElements: HTMLElement` — список товаров корзины.
`totalElements: HTMLElement` — итоговая сумма.
`orderButton: HTMLButtonElement` — кнопка оформления заказа.

Методы класса:
`items: HTMLElement[] (сеттер)` — рендерит список товаров.
`total: number (сеттер)` — обновляет итоговую сумму.
`buttonDisabled: boolean (сеттер)` — включает или выключает кнопку оформления.

#### Класс FormPayment
Форма оформления заказа.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — принимает DOM-элемент формы и брокер событий.

Поля класса:
`paymentButtons: HTMLButtonElement[]` — кнопки выбора способа оплаты.
`addressInput: HTMLInputElement` — поле ввода адреса.

Методы класса:
`payment: TPayment | null (сеттер)` — выделяет выбранный способ оплаты через модификатор button_alt-active или снимает выделение, если способ оплаты ещё не выбран.
`address: string (сеттер)` — устанавливает адрес доставки.

#### FormContacts
Форма ввода контактов покупателя.

Конструктор:
`constructor(container: HTMLFormElement, events: IEvents)` — принимает DOM-элемент формы и брокер событий.

Поля класса:
`emailInput: HTMLInputElement` — поле ввода e-mail.
`phoneInput: HTMLInputElement` — поле ввода телефона.

Методы класса:
`email: string (сеттер)` — устанавливает e-mail.
`phone: string (сеттер)` — устанавливает телефон.

#### SuccessView
Экран успешного оформления заказа.

Конструктор:
`constructor(container: HTMLElement, events: IEvents)` — принимает корневой элемент экрана успеха и брокер событий.

Поля класса:
`descriptionElement: HTMLElement` — текст с суммой заказа.
`closeButton: HTMLButtonElement` — кнопка «За новыми покупками!».

Методы класса:
`total: number (сеттер)` — обновляет текст с итоговой суммой.

### События приложения
События генерируются как View-компонентами, так и моделями данных. Все они обрабатываются Презентером.

`catalog:changed` — в модели каталога обновлён список товаров.
`catalog:previewChanged` — в модели каталога изменён товар предпросмотра.
`basket:changed` — в модели корзины изменился состав товаров.
`buyer:changed` — в модели покупателя изменились данные.

`card:select` — событие выбора карточки товара в каталоге.
`basket:item-delete` — событие удаления товара из корзины.
`basket:open` — пользователь нажал кнопку корзины в шапке.
`preview:toggle` — пользователь нажал кнопку в предпросмотре.
`basket:submit` — пользователь нажал кнопку перехода к оформлению заказа в корзине.
`order.payment:change` — пользователь выбрал способ оплаты.
`order.address:change` — пользователь изменил адрес доставки.
`order:submit` — пользователь отправил форму заказа.
`contacts.email:change` — пользователь изменил e-mail.
`contacts.phone:change` — пользователь изменил телефон.
`contacts:submit` — пользователь отправил форму контактов.
`modal:close` — пользователь запросил закрытие модального окна (кнопка, оверлей или Escape).
`success:close` — пользователь нажал кнопку закрытия экрана успешного заказа.

### Презентер
Презентер реализован в основном скрипте `src/main.ts`.

`main.ts` - создаёт экземпляры всех моделей, представлений, API-классов и общего брокера событий; подписывается на все события моделей и представлений; изменяет данные только через методы моделей и обновляет отображение только при обработке событий моделей или при открытии модального окна.

Основной поток работы: после загрузки товаров с сервера Презентер сохраняет их в `Products`, а затем по событию `catalog:changed` рендерит карточки каталога;
при создании `CardCatalog` и `CardBasket` Презентер передаёт готовые обработчики действий и не использует DOM как источник данных;
по событиям пользовательских действий Презентер изменяет состояние моделей (`Products`, `Basket`, `Buyer`);
по событиям моделей Презентер синхронизирует интерфейс: перерисовывает каталог, счётчик корзины, содержимое модального окна и формы;
при отправке первой формы Презентер просто открывает форму контактов, а при отправке второй формы собирает заказ, отправляет его через `WebLarekApi`, затем показывает экран успешного оформления и очищает модели корзины и покупателя.

### Слой коммуникации

#### Класс WebLarekApi
`WebLarekApi` — класс коммуникационного слоя, который работает через композицию с объектом, реализующим интерфейс IApi.

Конструктор:
`constructor(response: IApi)` — принимает зависимость для HTTP-запросов (объект, реализующий `IApi`) и сохраняет её в поле класса.

Методы: 
`getProducts(): Promise<IProduct[]>` — выполняет GET /product/, получает объект IProductsResponse и возвращает массив товаров items.

`sendOrder(order: IOrderRequest): Promise<IOrderResponse>` — выполняет POST /order/ и отправляет данные заказа в формате `IOrderRequest`:

данные покупателя (`IBuyer`),
список id товаров (`items: string[]`),
итоговую сумму (`total: number`).

Ответ сервера при оформлении заказа описан типом `IOrderResult` (`id` и `total`).